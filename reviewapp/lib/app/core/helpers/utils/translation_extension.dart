// AUTO-GENERATED FILE - DO NOT EDIT
import 'package:mon_etatsdeslieux/generated/l10n.dart' as l;
import 'package:flutter/material.dart';

extension ValueLocalization on Map<String, String> {
  Map<String, String> translatedValues(BuildContext context) {
    final t = l.S.of(context);
    return map((key, value) {
      switch (key) {
        case 'baseelements':
          return MapEntry(key, t.baseelements);
        case 'roomInventoriesDescription':
          return MapEntry(key, t.roomInventoriesDescription);
        case 'room':
          return MapEntry(key, t.room);
        case 'itemInventories':
          return MapEntry(key, t.itemInventories);
        case 'roomInventories':
          return MapEntry(key, t.roomInventories);
        case 'livingRoom':
          return MapEntry(key, t.livingRoom);
        case 'bedroom':
          return MapEntry(key, t.bedroom);
        case 'kitchen':
          return MapEntry(key, t.kitchen);
        case 'bathroom':
          return MapEntry(key, t.bathroom);
        case 'toilets':
          return MapEntry(key, t.toilets);
        case 'laundry':
          return MapEntry(key, t.laundry);
        case 'entrance':
          return MapEntry(key, t.entrance);
        case 'balcony':
          return MapEntry(key, t.balcony);
        case 'terrace':
          return MapEntry(key, t.terrace);
        case 'parkingGarage':
          return MapEntry(key, t.parkingGarage);
        case 'cellar':
          return MapEntry(key, t.cellar);
        case 'garden':
          return MapEntry(key, t.garden);
        case 'otherRoom':
          return MapEntry(key, t.otherRoom);
        case 'yes':
          return MapEntry(key, t.yes);
        case 'no':
          return MapEntry(key, t.no);
        case 'wantToDoInventory':
          return MapEntry(key, t.wantToDoInventory);
        case 'location':
          return MapEntry(key, t.location);
        case 'individual':
          return MapEntry(key, t.individual);
        case 'collective':
          return MapEntry(key, t.collective);
        case 'district':
          return MapEntry(key, t.district);
        case 'central':
          return MapEntry(key, t.central);
        case 'not_applicable':
          return MapEntry(key, t.not_applicable);
        case 'electric':
          return MapEntry(key, t.electric);
        case 'gas':
          return MapEntry(key, t.gas);
        case 'wood':
          return MapEntry(key, t.wood);
        case 'pellet':
          return MapEntry(key, t.pellet);
        case 'oil':
          return MapEntry(key, t.oil);
        case 'solar':
          return MapEntry(key, t.solar);
        case 'coal':
          return MapEntry(key, t.coal);
        case 'natural_gas':
          return MapEntry(key, t.natural_gas);
        case 'additionalInfo':
          return MapEntry(key, t.additionalInfo);
        case 'heatingType':
          return MapEntry(key, t.heatingType);
        case 'heatingMode':
          return MapEntry(key, t.heatingMode);
        case 'hotWaterType':
          return MapEntry(key, t.hotWaterType);
        case 'hotWaterMode':
          return MapEntry(key, t.hotWaterMode);
        case 'mailbox':
          return MapEntry(key, t.mailbox);
        case 'typeOfGood':
          return MapEntry(key, t.typeOfGood);
        case 'supply':
          return MapEntry(key, t.supply);
        case 'propertyLabel':
          return MapEntry(key, t.propertyLabel);
        case 'propertyAddress':
          return MapEntry(key, t.propertyAddress);
        case 'addressComplement':
          return MapEntry(key, t.addressComplement);
        case 'floor':
          return MapEntry(key, t.floor);
        case 'surface':
          return MapEntry(key, t.surface);
        case 'roomCount':
          return MapEntry(key, t.roomCount);
        case 'furnished':
          return MapEntry(key, t.furnished);
        case 'unfurnished':
          return MapEntry(key, t.unfurnished);
        case 'box':
          return MapEntry(key, t.box);
        case 'garage':
          return MapEntry(key, t.garage);
        case 'parking':
          return MapEntry(key, t.parking);
        case 'reviewDate':
          return MapEntry(key, t.reviewDate);
        case 'documentMadeAt':
          return MapEntry(key, t.documentMadeAt);
        case 'tenantExitDate':
          return MapEntry(key, t.tenantExitDate);
        case 'pleaseentervalidEmail':
          return MapEntry(key, t.pleaseentervalidEmail);
        case 'whereSearching':
          return MapEntry(key, t.whereSearching);
        case 'requiredInfo':
          return MapEntry(key, t.requiredInfo);
        case 'validEmail':
          return MapEntry(key, t.validEmail);
        case 'enterThe':
          return MapEntry(key, t.enterThe);
        case 'tenantInfo':
          return MapEntry(key, t.tenantInfo);
        case 'whoDoesReview':
          return MapEntry(key, t.whoDoesReview);
        case 'mandated':
          return MapEntry(key, t.mandated);
        case 'fullNameMandataire':
          return MapEntry(key, t.fullNameMandataire);
        case 'addressMandataire':
          return MapEntry(key, t.addressMandataire);
        case 'emailMandataire':
          return MapEntry(key, t.emailMandataire);
        case 'fullNameTenant':
          return MapEntry(key, t.fullNameTenant);
        case 'addressTenant':
          return MapEntry(key, t.addressTenant);
        case 'emailTenant':
          return MapEntry(key, t.emailTenant);
        case 'entryDateIfExit':
          return MapEntry(key, t.entryDateIfExit);
        case 'and':
          return MapEntry(key, t.and);
        case 'procurations':
          return MapEntry(key, t.procurations);
        case 'inventories':
          return MapEntry(key, t.inventories);
        case 'selfreview':
          return MapEntry(key, t.selfreview);
        case 'delegateedl':
          return MapEntry(key, t.delegateedl);
        case 'ownerinfo':
          return MapEntry(key, t.ownerinfo);
        case 'ownerfullname':
          return MapEntry(key, t.ownerfullname);
        case 'owneraddress':
          return MapEntry(key, t.owneraddress);
        case 'owneremail':
          return MapEntry(key, t.owneremail);
        case 'selectreviewtype':
          return MapEntry(key, t.selectreviewtype);
        case 'pleaseselecttype':
          return MapEntry(key, t.pleaseselecttype);
        case 'exitreview':
          return MapEntry(key, t.exitreview);
        case 'createinventoryreport':
          return MapEntry(key, t.createinventoryreport);
        case 'bestlandlords':
          return MapEntry(key, t.bestlandlords);
        case 'finalization':
          return MapEntry(key, t.finalization);
        case 'inventoryreport':
          return MapEntry(key, t.inventoryreport);
        case 'finished':
          return MapEntry(key, t.finished);
        case 'landlordprincipal':
          return MapEntry(key, t.landlordprincipal);
        case 'outgoingtenant':
          return MapEntry(key, t.outgoingtenant);
        case 'incomingtenant':
          return MapEntry(key, t.incomingtenant);
        case 'findproxies':
          return MapEntry(key, t.findproxies);
        case 'findreportshere':
          return MapEntry(key, t.findreportshere);
        case 'termsandconditions':
          return MapEntry(key, t.termsandconditions);
        case 'contactus':
          return MapEntry(key, t.contactus);
        case 'deleteaccount':
          return MapEntry(key, t.deleteaccount);
        case 'buycredits':
          return MapEntry(key, t.buycredits);
        case 'myprocurations':
          return MapEntry(key, t.myprocurations);
        case 'myreviews':
          return MapEntry(key, t.myreviews);
        case 'signmeout':
          return MapEntry(key, t.signmeout);
        case 'youwillbeloggedout':
          return MapEntry(key, t.youwillbeloggedout);
        case 'doyouwantToproceed':
          return MapEntry(key, t.doyouwantToproceed);
        case 'signout':
          return MapEntry(key, t.signout);
        case 'verificationInprogress':
          return MapEntry(key, t.verificationInprogress);
        case 'vevrify':
          return MapEntry(key, t.vevrify);
        case 'resendotp':
          return MapEntry(key, t.resendotp);
        case 'pleaseEnterYourPassword':
          return MapEntry(key, t.pleaseEnterYourPassword);
        case 'tenant':
          return MapEntry(key, t.tenant);
        case 'otpverification':
          return MapEntry(key, t.otpverification);
        case 'wesendandotpto':
          return MapEntry(key, t.wesendandotpto);
        case 'entertoconfirm':
          return MapEntry(key, t.entertoconfirm);
        case 'passmustbeatleastsixcharaters':
          return MapEntry(key, t.passmustbeatleastsixcharaters);
        case 'iam':
          return MapEntry(key, t.iam);
        case 'owner':
          return MapEntry(key, t.owner);
        case 'hello_world':
          return MapEntry(key, t.hello_world);
        case 'solidAlerts':
          return MapEntry(key, t.solidAlerts);
        case 'thisIsAPrimaryAlert':
          return MapEntry(key, t.thisIsAPrimaryAlert);
        case 'thisIsASecondaryAlert':
          return MapEntry(key, t.thisIsASecondaryAlert);
        case 'thisIsASuccessAlert':
          return MapEntry(key, t.thisIsASuccessAlert);
        case 'thisIsAWarningAlert':
          return MapEntry(key, t.thisIsAWarningAlert);
        case 'thisIsAInfoAlert':
          return MapEntry(key, t.thisIsAInfoAlert);
        case 'thisIsADangerAlert':
          return MapEntry(key, t.thisIsADangerAlert);
        case 'leftBorderAlerts':
          return MapEntry(key, t.leftBorderAlerts);
        case 'outlineAlerts':
          return MapEntry(key, t.outlineAlerts);
        case 'signIn':
          return MapEntry(key, t.signIn);
        case 'needAnAccount':
          return MapEntry(key, t.needAnAccount);
        case 'signUp':
          return MapEntry(key, t.signUp);
        case 'useGoogle':
          return MapEntry(key, t.useGoogle);
        case 'useApple':
          return MapEntry(key, t.useApple);
        case 'or':
          return MapEntry(key, t.or);
        case 'email':
          return MapEntry(key, t.email);
        case 'enterYourEmailAddress':
          return MapEntry(key, t.enterYourEmailAddress);
        case 'password':
          return MapEntry(key, t.password);
        case 'enterYourPassword':
          return MapEntry(key, t.enterYourPassword);
        case 'rememberMe':
          return MapEntry(key, t.rememberMe);
        case 'forgotPassword':
          return MapEntry(key, t.forgotPassword);
        case 'enterYourEmailWeWillSendYouALinkToResetYourPassword':
          return MapEntry(
            key,
            t.enterYourEmailWeWillSendYouALinkToResetYourPassword,
          );
        case 'send':
          return MapEntry(key, t.send);
        case 'shapesStyles':
          return MapEntry(key, t.shapesStyles);
        case 'indicators':
          return MapEntry(key, t.indicators);
        case 'avatarWithContent':
          return MapEntry(key, t.avatarWithContent);
        case 'avatarGroup':
          return MapEntry(key, t.avatarGroup);
        case 'type':
          return MapEntry(key, t.type);
        case 'sM':
          return MapEntry(key, t.sM);
        case 'basic':
          return MapEntry(key, t.basic);
        case 'defaultButtons':
          return MapEntry(key, t.defaultButtons);
        case 'primary':
          return MapEntry(key, t.primary);
        case 'background':
          return MapEntry(key, t.background);
        case 'foreground':
          return MapEntry(key, t.foreground);
        case 'secondary':
          return MapEntry(key, t.secondary);
        case 'info':
          return MapEntry(key, t.info);
        case 'warning':
          return MapEntry(key, t.warning);
        case 'danger':
          return MapEntry(key, t.danger);
        case 'dark':
          return MapEntry(key, t.dark);
        case 'link':
          return MapEntry(key, t.link);
        case 'light':
          return MapEntry(key, t.light);
        case 'outlineButtons':
          return MapEntry(key, t.outlineButtons);
        case 'softButtons':
          return MapEntry(key, t.softButtons);
        case 'ghostButtons':
          return MapEntry(key, t.ghostButtons);
        case 'buttonsWithLabel':
          return MapEntry(key, t.buttonsWithLabel);
        case 'loadMoreButtons':
          return MapEntry(key, t.loadMoreButtons);
        case 'loading':
          return MapEntry(key, t.loading);
        case 'buttonsSizes':
          return MapEntry(key, t.buttonsSizes);
        case 'button2Xl':
          return MapEntry(key, t.button2Xl);
        case 'padding':
          return MapEntry(key, t.padding);
        case 'buttonXl':
          return MapEntry(key, t.buttonXl);
        case 'buttonLg':
          return MapEntry(key, t.buttonLg);
        case 'buttonMd':
          return MapEntry(key, t.buttonMd);
        case 'buttonSm':
          return MapEntry(key, t.buttonSm);
        case 'groupButtons':
          return MapEntry(key, t.groupButtons);
        case 'buttonsToolbar':
          return MapEntry(key, t.buttonsToolbar);
        case 'verticalVariation':
          return MapEntry(key, t.verticalVariation);
        case 'button':
          return MapEntry(key, t.button);
        case 'dropdown':
          return MapEntry(key, t.dropdown);
        case 'item':
          return MapEntry(key, t.item);
        case 'success':
          return MapEntry(key, t.success);
        case 'addNewTask':
          return MapEntry(key, t.addNewTask);
        case 'title':
          return MapEntry(key, t.title);
        case 'enterTitle':
          return MapEntry(key, t.enterTitle);
        case 'startDate':
          return MapEntry(key, t.startDate);
        case 'selectStartDate':
          return MapEntry(key, t.selectStartDate);
        case 'time':
          return MapEntry(key, t.time);
        case 'selectStartTime':
          return MapEntry(key, t.selectStartTime);
        case 'endDate':
          return MapEntry(key, t.endDate);
        case 'selectEndDate':
          return MapEntry(key, t.selectEndDate);
        case 'selectEndTime':
          return MapEntry(key, t.selectEndTime);
        case 'description':
          return MapEntry(key, t.description);
        case 'enterHere':
          return MapEntry(key, t.enterHere);
        case 'cancel':
          return MapEntry(key, t.cancel);
        case 'save':
          return MapEntry(key, t.save);
        case 'viewDetails':
          return MapEntry(key, t.viewDetails);
        case 'startTime':
          return MapEntry(key, t.startTime);
        case 'endTime':
          return MapEntry(key, t.endTime);
        case 'details':
          return MapEntry(key, t.details);
        case 'daily':
          return MapEntry(key, t.daily);
        case 'weekly':
          return MapEntry(key, t.weekly);
        case 'month':
          return MapEntry(key, t.month);
        case 'add':
          return MapEntry(key, t.add);
        case 'newTask':
          return MapEntry(key, t.newTask);
        case 'uIUXDesignReview':
          return MapEntry(key, t.uIUXDesignReview);
        case 'writeEngagingIntroductionSectionParagraphsForYourBlog':
          return MapEntry(
            key,
            t.writeEngagingIntroductionSectionParagraphsForYourBlog,
          );
        case 'businessIdeas':
          return MapEntry(key, t.businessIdeas);
        case 'blogPostWriting':
          return MapEntry(key, t.blogPostWriting);
        case 'technology':
          return MapEntry(key, t.technology);
        case 'paragraphGenerator':
          return MapEntry(key, t.paragraphGenerator);
        case 'horizontalCards':
          return MapEntry(key, t.horizontalCards);
        case 'completed':
          return MapEntry(key, t.completed);
        case 'cardImage':
          return MapEntry(key, t.cardImage);
        case 'cardBorderColor':
          return MapEntry(key, t.cardBorderColor);
        case 'selectAUserToStartChatting':
          return MapEntry(key, t.selectAUserToStartChatting);
        case 'active':
          return MapEntry(key, t.active);
        case 'createNewGroup':
          return MapEntry(key, t.createNewGroup);
        case 'deleteConversation':
          return MapEntry(key, t.deleteConversation);
        case 'blockReport':
          return MapEntry(key, t.blockReport);
        case 'submit':
          return MapEntry(key, t.submit);
        case 'typeHere':
          return MapEntry(key, t.typeHere);
        case 'search':
          return MapEntry(key, t.search);
        case 'private':
          return MapEntry(key, t.private);
        case 'group':
          return MapEntry(key, t.group);
        case 'all':
          return MapEntry(key, t.all);
        case 'heyThereIm':
          return MapEntry(key, t.heyThereIm);
        case 'colorsOptions':
          return MapEntry(key, t.colorsOptions);
        case 'income':
          return MapEntry(key, t.income);
        case 'expense':
          return MapEntry(key, t.expense);
        case 'incomeVSExpense':
          return MapEntry(key, t.incomeVSExpense);
        case 'newOrders':
          return MapEntry(key, t.newOrders);
        case 'status':
          return MapEntry(key, t.status);
        case 'approved':
          return MapEntry(key, t.approved);
        case 'pending':
          return MapEntry(key, t.pending);
        case 'eCommerceAdminDashboardViewIsWorking':
          return MapEntry(key, t.eCommerceAdminDashboardViewIsWorking);
        case 'hrmAdminDashboardIsWorking':
          return MapEntry(key, t.hrmAdminDashboardIsWorking);
        case 'influencerAdminDashboardIsWorking':
          return MapEntry(key, t.influencerAdminDashboardIsWorking);
        case 'newsAdminDashboardIsWorking':
          return MapEntry(key, t.newsAdminDashboardIsWorking);
        case 'wordGeneration':
          return MapEntry(key, t.wordGeneration);
        case 'contentsOverviews':
          return MapEntry(key, t.contentsOverviews);
        case 'subscribeStatistic':
          return MapEntry(key, t.subscribeStatistic);
        case 'rewardEarningAdminDashboardIsWorking':
          return MapEntry(key, t.rewardEarningAdminDashboardIsWorking);
        case 'sMSAdminDashboardIsWorking':
          return MapEntry(key, t.sMSAdminDashboardIsWorking);
        case 'tableDragDrop':
          return MapEntry(key, t.tableDragDrop);
        case 'inactive':
          return MapEntry(key, t.inactive);
        case 'inbox':
          return MapEntry(key, t.inbox);
        case 'starred':
          return MapEntry(key, t.starred);
        case 'sent':
          return MapEntry(key, t.sent);
        case 'drafts':
          return MapEntry(key, t.drafts);
        case 'spam':
          return MapEntry(key, t.spam);
        case 'trash':
          return MapEntry(key, t.trash);
        case 'compose':
          return MapEntry(key, t.compose);
        case 'tags':
          return MapEntry(key, t.tags);
        case 'newtext':
          return MapEntry(key, t.newtext);
        case 'openNavigationMenu':
          return MapEntry(key, t.openNavigationMenu);
        case 'whimsicalWeddingGraphicsToDesignYour':
          return MapEntry(key, t.whimsicalWeddingGraphicsToDesignYour);
        case 'devonLane':
          return MapEntry(key, t.devonLane);
        case 'reply':
          return MapEntry(key, t.reply);
        case 'forward':
          return MapEntry(key, t.forward);
        case 'generalQuestions':
          return MapEntry(key, t.generalQuestions);
        case 'howCanYouBeSureTheNumbersAreReallyRandom':
          return MapEntry(key, t.howCanYouBeSureTheNumbersAreReallyRandom);
        case 'isTheSourceCodeForTheGeneratorAvailable':
          return MapEntry(key, t.isTheSourceCodeForTheGeneratorAvailable);
        case 'canIDownloadTheGeneratorSoftwareAndRunItOnMyOwnComputer':
          return MapEntry(
            key,
            t.canIDownloadTheGeneratorSoftwareAndRunItOnMyOwnComputer,
          );
        case 'canOtherInfoBeAddedToAnInvoice':
          return MapEntry(key, t.canOtherInfoBeAddedToAnInvoice);
        case 'howDoIPickWinnersForALotteryOrDrawing':
          return MapEntry(key, t.howDoIPickWinnersForALotteryOrDrawing);
        case 'whatSecurityMeasuresAreInPlaceForMyData':
          return MapEntry(key, t.whatSecurityMeasuresAreInPlaceForMyData);
        case 'whatShouldIDoIfIEncounterTechnicalIssues':
          return MapEntry(key, t.whatShouldIDoIfIEncounterTechnicalIssues);
        case 'ourGeneratorSourceCodeIsProprietaryAndNotPubliclyAvailable':
          return MapEntry(
            key,
            t.ourGeneratorSourceCodeIsProprietaryAndNotPubliclyAvailable,
          );
        case 'weCombineHardwareAndSoftwareMethods':
          return MapEntry(key, t.weCombineHardwareAndSoftwareMethods);
        case 'theGeneratorOperatesOnlyOnOurSecureOnlinePlatform':
          return MapEntry(
            key,
            t.theGeneratorOperatesOnlyOnOurSecureOnlinePlatform,
          );
        case 'ourInvoicingSystemAllowsForCustomization':
          return MapEntry(key, t.ourInvoicingSystemAllowsForCustomization);
        case 'weUseCryptographicAlgorithmsAndRigorousTesting':
          return MapEntry(
            key,
            t.weUseCryptographicAlgorithmsAndRigorousTesting,
          );
        case 'useACertifiedRandomNumberGeneratorForAFair':
          return MapEntry(key, t.useACertifiedRandomNumberGeneratorForAFair);
        case 'weImplementRobustSecurityMeasuresIncludingEncryption':
          return MapEntry(
            key,
            t.weImplementRobustSecurityMeasuresIncludingEncryption,
          );
        case 'ifYouExperienceTechnicalIssues':
          return MapEntry(key, t.ifYouExperienceTechnicalIssues);
        case 'alreadyHaveAnAccount':
          return MapEntry(key, t.alreadyHaveAnAccount);
        case 'fullName':
          return MapEntry(key, t.fullName);
        case 'enterFullName':
          return MapEntry(key, t.enterFullName);
        case 'enterEmailAddress':
          return MapEntry(key, t.enterEmailAddress);
        case 'sales':
          return MapEntry(key, t.sales);
        case 'sale':
          return MapEntry(key, t.sale);
        case 'blackTShirtForMan':
          return MapEntry(key, t.blackTShirtForMan);
        case 'salesRatio':
          return MapEntry(key, t.salesRatio);
        case 'salesByCountry':
          return MapEntry(key, t.salesByCountry);
        case 'registeredOn':
          return MapEntry(key, t.registeredOn);
        case 'userName':
          return MapEntry(key, t.userName);
        case 'phone':
          return MapEntry(key, t.phone);
        case 'position':
          return MapEntry(key, t.position);
        case 'action':
          return MapEntry(key, t.action);
        case 'inputExample':
          return MapEntry(key, t.inputExample);
        case 'inputWithSelect':
          return MapEntry(key, t.inputWithSelect);
        case 'select':
          return MapEntry(key, t.select);
        case 'disabledInput':
          return MapEntry(key, t.disabledInput);
        case 'inputWithIcon':
          return MapEntry(key, t.inputWithIcon);
        case 'inputWithIconRight':
          return MapEntry(key, t.inputWithIconRight);
        case 'inputPassword':
          return MapEntry(key, t.inputPassword);
        case 'inputDate':
          return MapEntry(key, t.inputDate);
        case 'roundedInput':
          return MapEntry(key, t.roundedInput);
        case 'inputBorderStyle':
          return MapEntry(key, t.inputBorderStyle);
        case 'exampleTextarea':
          return MapEntry(key, t.exampleTextarea);
        case 'inputSizing':
          return MapEntry(key, t.inputSizing);
        case 'fileInput':
          return MapEntry(key, t.fileInput);
        case 'defaultSelect':
          return MapEntry(key, t.defaultSelect);
        case 'inputWithPlaceholder':
          return MapEntry(key, t.inputWithPlaceholder);
        case 'selectYouStatus':
          return MapEntry(key, t.selectYouStatus);
        case 'menuSize':
          return MapEntry(key, t.menuSize);
        case 'selectSize':
          return MapEntry(key, t.selectSize);
        case 'multipleSelectInput':
          return MapEntry(key, t.multipleSelectInput);
        case 'defaulttext':
          return MapEntry(key, t.defaulttext);
        case 'selectItems':
          return MapEntry(key, t.selectItems);
        case 'choseACity':
          return MapEntry(key, t.choseACity);
        case 'textInputs':
          return MapEntry(key, t.textInputs);
        case 'browserDefaults':
          return MapEntry(key, t.browserDefaults);
        case 'firstName':
          return MapEntry(key, t.firstName);
        case 'enterYourFirstName':
          return MapEntry(key, t.enterYourFirstName);
        case 'pleaseEnterYourFirstName':
          return MapEntry(key, t.pleaseEnterYourFirstName);
        case 'lastName':
          return MapEntry(key, t.lastName);
        case 'enterYourLastName':
          return MapEntry(key, t.enterYourLastName);
        case 'pleaseEnterYourLastName':
          return MapEntry(key, t.pleaseEnterYourLastName);
        case 'country':
          return MapEntry(key, t.country);
        case 'selectCountry':
          return MapEntry(key, t.selectCountry);
        case 'pleaseSelectYourCountry':
          return MapEntry(key, t.pleaseSelectYourCountry);
        case 'city':
          return MapEntry(key, t.city);
        case 'selectCity':
          return MapEntry(key, t.selectCity);
        case 'pleaseSelectYourCity':
          return MapEntry(key, t.pleaseSelectYourCity);
        case 'state':
          return MapEntry(key, t.state);
        case 'selectState':
          return MapEntry(key, t.selectState);
        case 'pleaseSelectYourState':
          return MapEntry(key, t.pleaseSelectYourState);
        case 'agreeToTermsAndConditions':
          return MapEntry(key, t.agreeToTermsAndConditions);
        case 'pleaseCheckThisBoxToContinue':
          return MapEntry(key, t.pleaseCheckThisBoxToContinue);
        case 'saveFrom':
          return MapEntry(key, t.saveFrom);
        case 'customStyles':
          return MapEntry(key, t.customStyles);
        case 'business':
          return MapEntry(key, t.business);
        case 'personal':
          return MapEntry(key, t.personal);
        case 'uIUXDesign':
          return MapEntry(key, t.uIUXDesign);
        case 'development':
          return MapEntry(key, t.development);
        case 'noDataFound':
          return MapEntry(key, t.noDataFound);
        case 'assignedTo':
          return MapEntry(key, t.assignedTo);
        case 'day':
          return MapEntry(key, t.day);
        case 'days':
          return MapEntry(key, t.days);
        case 'left':
          return MapEntry(key, t.left);
        case 'createProject':
          return MapEntry(key, t.createProject);
        case 'projectName':
          return MapEntry(key, t.projectName);
        case 'enterProjectName':
          return MapEntry(key, t.enterProjectName);
        case 'pleaseEnterProjectName':
          return MapEntry(key, t.pleaseEnterProjectName);
        case 'pleaseSelectStartDate':
          return MapEntry(key, t.pleaseSelectStartDate);
        case 'pleaseSelectEndDate':
          return MapEntry(key, t.pleaseSelectEndDate);
        case 'createNewBoard':
          return MapEntry(key, t.createNewBoard);
        case 'boardName':
          return MapEntry(key, t.boardName);
        case 'enterBoardName':
          return MapEntry(key, t.enterBoardName);
        case 'pleaseEnterBoardName':
          return MapEntry(key, t.pleaseEnterBoardName);
        case 'selectColor':
          return MapEntry(key, t.selectColor);
        case 'addNewBoard':
          return MapEntry(key, t.addNewBoard);
        case 'googleMap':
          return MapEntry(key, t.googleMap);
        case 'defaultMap':
          return MapEntry(key, t.defaultMap);
        case 'leafletMultipleLocation':
          return MapEntry(key, t.leafletMultipleLocation);
        case 'ooopsPageNotFound':
          return MapEntry(key, t.ooopsPageNotFound);
        case 'thisPageDoesNotExist':
          return MapEntry(key, t.thisPageDoesNotExist);
        case 'goBack':
          return MapEntry(key, t.goBack);
        case 'newConversation':
          return MapEntry(key, t.newConversation);
        case 'message':
          return MapEntry(key, t.message);
        case 'hoursAgo':
          return MapEntry(key, t.hoursAgo);
        case 'typeAMessageOrUploadAnImage':
          return MapEntry(key, t.typeAMessageOrUploadAnImage);
        case 'aiChatbot':
          return MapEntry(key, t.aiChatbot);
        case 'defaultBot':
          return MapEntry(key, t.defaultBot);
        case 'basicPricingPlan':
          return MapEntry(key, t.basicPricingPlan);
        case 'chooseYourBestPlan':
          return MapEntry(key, t.chooseYourBestPlan);
        case 'defaultPricingPlan':
          return MapEntry(key, t.defaultPricingPlan);
        case 'powerfulFeaturesFor':
          return MapEntry(key, t.powerfulFeaturesFor);
        case 'powerfulCreators':
          return MapEntry(key, t.powerfulCreators);
        case 'payMonthly':
          return MapEntry(key, t.payMonthly);
        case 'payYearly':
          return MapEntry(key, t.payYearly);
        case 'alice':
          return MapEntry(key, t.alice);
        case 'bob':
          return MapEntry(key, t.bob);
        case 'addNewProject':
          return MapEntry(key, t.addNewProject);
        case 'enterProjectTitle':
          return MapEntry(key, t.enterProjectTitle);
        case 'pleaseEnterTitle':
          return MapEntry(key, t.pleaseEnterTitle);
        case 'assignerTo':
          return MapEntry(key, t.assignerTo);
        case 'selectEmployee':
          return MapEntry(key, t.selectEmployee);
        case 'writeSomething':
          return MapEntry(key, t.writeSomething);
        case 'inProgress':
          return MapEntry(key, t.inProgress);
        case 'allPriority':
          return MapEntry(key, t.allPriority);
        case 'low':
          return MapEntry(key, t.low);
        case 'medium':
          return MapEntry(key, t.medium);
        case 'high':
          return MapEntry(key, t.high);
        case 'showing':
          return MapEntry(key, t.showing);
        case 'entries':
          return MapEntry(key, t.entries);
        case 'addNewUser':
          return MapEntry(key, t.addNewUser);
        case 'previous':
          return MapEntry(key, t.previous);
        case 'next':
          return MapEntry(key, t.next);
        case 'show':
          return MapEntry(key, t.show);
        case 'project':
          return MapEntry(key, t.project);
        case 'priority':
          return MapEntry(key, t.priority);
        case 'actions':
          return MapEntry(key, t.actions);
        case 'edit':
          return MapEntry(key, t.edit);
        case 'cOPYRIGHT':
          return MapEntry(key, t.cOPYRIGHT);
        case 'allRightsReserved':
          return MapEntry(key, t.allRightsReserved);
        case 'unknownRoute':
          return MapEntry(key, t.unknownRoute);
        case 'borderedTable':
          return MapEntry(key, t.borderedTable);
        case 'stripedRows':
          return MapEntry(key, t.stripedRows);
        case 'hoverTable':
          return MapEntry(key, t.hoverTable);
        case 'tablesBorderColors':
          return MapEntry(key, t.tablesBorderColors);
        case 'tableHead':
          return MapEntry(key, t.tableHead);
        case 'name':
          return MapEntry(key, t.name);
        case 'sl':
          return MapEntry(key, t.sl);
        case 'defaultTable':
          return MapEntry(key, t.defaultTable);
        case 'profile':
          return MapEntry(key, t.profile);
        case 'settings':
          return MapEntry(key, t.settings);
        case 'whatIsLoremIpsum':
          return MapEntry(key, t.whatIsLoremIpsum);
        case 'heading':
          return MapEntry(key, t.heading);
        case 'headingColors':
          return MapEntry(key, t.headingColors);
        case 'texts':
          return MapEntry(key, t.texts);
        case 'inineTextElements':
          return MapEntry(key, t.inineTextElements);
        case 'youCanUseTheMarkTagTo':
          return MapEntry(key, t.youCanUseTheMarkTagTo);
        case 'highlight':
          return MapEntry(key, t.highlight);
        case 'text':
          return MapEntry(key, t.text);
        case 'thisIsAParagraphItStandsOutFromRegularDeleteText':
          return MapEntry(
            key,
            t.thisIsAParagraphItStandsOutFromRegularDeleteText,
          );
        case 'thisLineOfTextIsMeantToBeTreatedAsNoLongerAccurate':
          return MapEntry(
            key,
            t.thisLineOfTextIsMeantToBeTreatedAsNoLongerAccurate,
          );
        case 'thisLineOfTextWillRenderAsUnderlined':
          return MapEntry(key, t.thisLineOfTextWillRenderAsUnderlined);
        case 'thisLineOfTextIsMeantToBeTreatedAsAnAdditionToTheDocument':
          return MapEntry(
            key,
            t.thisLineOfTextIsMeantToBeTreatedAsAnAdditionToTheDocument,
          );
        case 'thisIsAParagraphItStandsOutFromRegularText':
          return MapEntry(key, t.thisIsAParagraphItStandsOutFromRegularText);
        case 'thisLineRenderedAsBoldText':
          return MapEntry(key, t.thisLineRenderedAsBoldText);
        case 'thisLineRenderedAsItalicizedText':
          return MapEntry(key, t.thisLineRenderedAsItalicizedText);
        case 'serial':
          return MapEntry(key, t.serial);
        case 'manager':
          return MapEntry(key, t.manager);
        case 'developer':
          return MapEntry(key, t.developer);
        case 'designer':
          return MapEntry(key, t.designer);
        case 'tester':
          return MapEntry(key, t.tester);
        case 'formDialog':
          return MapEntry(key, t.formDialog);
        case 'image':
          return MapEntry(key, t.image);
        case 'uploadImage':
          return MapEntry(key, t.uploadImage);
        case 'selectPosition':
          return MapEntry(key, t.selectPosition);
        case 'pleaseSelectAPosition':
          return MapEntry(key, t.pleaseSelectAPosition);
        case 'enterYourFullName':
          return MapEntry(key, t.enterYourFullName);
        case 'pleaseEnterYourFullName':
          return MapEntry(key, t.pleaseEnterYourFullName);
        case 'enterYourEmail':
          return MapEntry(key, t.enterYourEmail);
        case 'pleaseEnterYourEmail':
          return MapEntry(key, t.pleaseEnterYourEmail);
        case 'phoneNumber':
          return MapEntry(key, t.phoneNumber);
        case 'enterYourPhoneNumber':
          return MapEntry(key, t.enterYourPhoneNumber);
        case 'pleaseEnterYourPhoneNumber':
          return MapEntry(key, t.pleaseEnterYourPhoneNumber);
        case 'note':
          return MapEntry(key, t.note);
        case 'userProfile':
          return MapEntry(key, t.userProfile);
        case 'registrationDate':
          return MapEntry(key, t.registrationDate);
        case 'profilePicture':
          return MapEntry(key, t.profilePicture);
        case 'currentPassword':
          return MapEntry(key, t.currentPassword);
        case 'newPassword':
          return MapEntry(key, t.newPassword);
        case 'confirmPassword':
          return MapEntry(key, t.confirmPassword);
        case 'saveChanges':
          return MapEntry(key, t.saveChanges);
        case 'calendar':
          return MapEntry(key, t.calendar);
        case 'socialMediaOverview':
          return MapEntry(key, t.socialMediaOverview);
        case 'by':
          return MapEntry(key, t.by);
        case 'statistic':
          return MapEntry(key, t.statistic);
        case 'stockValue':
          return MapEntry(key, t.stockValue);
        case 'lowStock':
          return MapEntry(key, t.lowStock);
        case 'topSellingProduct':
          return MapEntry(key, t.topSellingProduct);
        case 'customerOfTheMonth':
          return MapEntry(key, t.customerOfTheMonth);
        case 'top5PurchasingProduct':
          return MapEntry(key, t.top5PurchasingProduct);
        case 'totalOrders':
          return MapEntry(key, t.totalOrders);
        case 'runningOrder':
          return MapEntry(key, t.runningOrder);
        case 'pendingOrder':
          return MapEntry(key, t.pendingOrder);
        case 'weeklyValue':
          return MapEntry(key, t.weeklyValue);
        case 'monthlyValue':
          return MapEntry(key, t.monthlyValue);
        case 'yearlyValue':
          return MapEntry(key, t.yearlyValue);
        case 'cashBalance':
          return MapEntry(key, t.cashBalance);
        case 'bankBalance':
          return MapEntry(key, t.bankBalance);
        case 'supplierDue':
          return MapEntry(key, t.supplierDue);
        case 'monthlyExpense':
          return MapEntry(key, t.monthlyExpense);
        case 'debitTransaction':
          return MapEntry(key, t.debitTransaction);
        case 'creditTransaction':
          return MapEntry(key, t.creditTransaction);
        case 'generatedArticle':
          return MapEntry(key, t.generatedArticle);
        case 'speechToText':
          return MapEntry(key, t.speechToText);
        case 'imagesGenerated':
          return MapEntry(key, t.imagesGenerated);
        case 'pDFGenerate':
          return MapEntry(key, t.pDFGenerate);
        case 'codeGenerated':
          return MapEntry(key, t.codeGenerated);
        case 'voiceoverGenerated':
          return MapEntry(key, t.voiceoverGenerated);
        case 'documentGenerated':
          return MapEntry(key, t.documentGenerated);
        case 'totalCreditBalance':
          return MapEntry(key, t.totalCreditBalance);
        case 'apple':
          return MapEntry(key, t.apple);
        case 'bananas':
          return MapEntry(key, t.bananas);
        case 'freshSoyabeanOil':
          return MapEntry(key, t.freshSoyabeanOil);
        case 'cabbage':
          return MapEntry(key, t.cabbage);
        case 'rice':
          return MapEntry(key, t.rice);
        case 'freshFruits':
          return MapEntry(key, t.freshFruits);
        case 'beefMeat':
          return MapEntry(key, t.beefMeat);
        case 'beetroot':
          return MapEntry(key, t.beetroot);
        case 'potato':
          return MapEntry(key, t.potato);
        case 'date':
          return MapEntry(key, t.date);
        case 'invoice':
          return MapEntry(key, t.invoice);
        case 'partyName':
          return MapEntry(key, t.partyName);
        case 'paymentType':
          return MapEntry(key, t.paymentType);
        case 'amount':
          return MapEntry(key, t.amount);
        case 'paid':
          return MapEntry(key, t.paid);
        case 'due':
          return MapEntry(key, t.due);
        case 'estherHoward':
          return MapEntry(key, t.estherHoward);
        case 'cash':
          return MapEntry(key, t.cash);
        case 'unpaid':
          return MapEntry(key, t.unpaid);
        case 'jeromeBell':
          return MapEntry(key, t.jeromeBell);
        case 'marvinMcKinney':
          return MapEntry(key, t.marvinMcKinney);
        case 'kathrynMurphy':
          return MapEntry(key, t.kathrynMurphy);
        case 'floydMiles':
          return MapEntry(key, t.floydMiles);
        case 'recentSales':
          return MapEntry(key, t.recentSales);
        case 'show5Results':
          return MapEntry(key, t.show5Results);
        case 'viewAll':
          return MapEntry(key, t.viewAll);
        case 'totalSales':
          return MapEntry(key, t.totalSales);
        case 'thisMonth':
          return MapEntry(key, t.thisMonth);
        case 'totalProfit':
          return MapEntry(key, t.totalProfit);
        case 'cocaCola':
          return MapEntry(key, t.cocaCola);
        case 'drinks':
          return MapEntry(key, t.drinks);
        case 'fanta':
          return MapEntry(key, t.fanta);
        case 'fruits':
          return MapEntry(key, t.fruits);
        case 'bread':
          return MapEntry(key, t.bread);
        case 'food':
          return MapEntry(key, t.food);
        case 'jennyWilson':
          return MapEntry(key, t.jennyWilson);
        case 'dianneRussell':
          return MapEntry(key, t.dianneRussell);
        case 'wadeWarren':
          return MapEntry(key, t.wadeWarren);
        case 'darrellSteward':
          return MapEntry(key, t.darrellSteward);
        case 'dessieCooper':
          return MapEntry(key, t.dessieCooper);
        case 'mango':
          return MapEntry(key, t.mango);
        case 'bessieCooper':
          return MapEntry(key, t.bessieCooper);
        case 'banana':
          return MapEntry(key, t.banana);
        case 'helloAcnoo':
          return MapEntry(key, t.helloAcnoo);
        case 'bestRegards':
          return MapEntry(key, t.bestRegards);
        case 'acnoo':
          return MapEntry(key, t.acnoo);
        case 'basicInput':
          return MapEntry(key, t.basicInput);
        case 'inputWithValue':
          return MapEntry(key, t.inputWithValue);
        case 'rounded':
          return MapEntry(key, t.rounded);
        case 'placeholder':
          return MapEntry(key, t.placeholder);
        case 'inputValue':
          return MapEntry(key, t.inputValue);
        case 'input':
          return MapEntry(key, t.input);
        case 'openThisSelectMenu':
          return MapEntry(key, t.openThisSelectMenu);
        case 'loremIpsumIsSimplyDummyTextOfThePrintingAndTypesettingIndustry':
          return MapEntry(
            key,
            t.loremIpsumIsSimplyDummyTextOfThePrintingAndTypesettingIndustry,
          );
        case 'one':
          return MapEntry(key, t.one);
        case 'two':
          return MapEntry(key, t.two);
        case 'three':
          return MapEntry(key, t.three);
        case 'eligendiDoloreVoluptate':
          return MapEntry(key, t.eligendiDoloreVoluptate);
        case 'natusQuiaAut':
          return MapEntry(key, t.natusQuiaAut);
        case 'aliquamSintLibero':
          return MapEntry(key, t.aliquamSintLibero);
        case 'reiciendisRemVoluptas':
          return MapEntry(key, t.reiciendisRemVoluptas);
        case 'repellendusEumEveniet':
          return MapEntry(key, t.repellendusEumEveniet);
        case 'doloribuseEtEos':
          return MapEntry(key, t.doloribuseEtEos);
        case 'illoExcepturiCupiditate':
          return MapEntry(key, t.illoExcepturiCupiditate);
        case 'sitSitAut':
          return MapEntry(key, t.sitSitAut);
        case 'autemOptioVelit':
          return MapEntry(key, t.autemOptioVelit);
        case 'quiaExplicaboPossimus':
          return MapEntry(key, t.quiaExplicaboPossimus);
        case 'ipsumAspernaturId':
          return MapEntry(key, t.ipsumAspernaturId);
        case 'etEaQuis':
          return MapEntry(key, t.etEaQuis);
        case 'rerumSintAliquid':
          return MapEntry(key, t.rerumSintAliquid);
        case 'quiIsteRatione':
          return MapEntry(key, t.quiIsteRatione);
        case 'debitisUndeCorrupti':
          return MapEntry(key, t.debitisUndeCorrupti);
        case 'quiaEosEum':
          return MapEntry(key, t.quiaEosEum);
        case 'nihilPorroEst':
          return MapEntry(key, t.nihilPorroEst);
        case 'doloremSuntCorrupti':
          return MapEntry(key, t.doloremSuntCorrupti);
        case 'quiaNecessitatibusMolestiae':
          return MapEntry(key, t.quiaNecessitatibusMolestiae);
        case 'quiMolestiaeQuis':
          return MapEntry(key, t.quiMolestiaeQuis);
        case 'view':
          return MapEntry(key, t.view);
        case 'delete':
          return MapEntry(key, t.delete);
        case 'progress':
          return MapEntry(key, t.progress);
        case 'duration':
          return MapEntry(key, t.duration);
        case 'moveItemFrom':
          return MapEntry(key, t.moveItemFrom);
        case 'to':
          return MapEntry(key, t.to);
        case 'move':
          return MapEntry(key, t.move);
        case 'exportExcel':
          return MapEntry(key, t.exportExcel);
        case 'exportCSV':
          return MapEntry(key, t.exportCSV);
        case 'exportPDF':
          return MapEntry(key, t.exportPDF);
        case 'print':
          return MapEntry(key, t.print);
        case 'copy':
          return MapEntry(key, t.copy);
        case 'apply':
          return MapEntry(key, t.apply);
        case 'describeWhatKindOfCodeYouNeed':
          return MapEntry(key, t.describeWhatKindOfCodeYouNeed);
        case 'codingLevel':
          return MapEntry(key, t.codingLevel);
        case 'programingLanguage':
          return MapEntry(key, t.programingLanguage);
        case 'beginner':
          return MapEntry(key, t.beginner);
        case 'intermediate':
          return MapEntry(key, t.intermediate);
        case 'advanced':
          return MapEntry(key, t.advanced);
        case 'expert':
          return MapEntry(key, t.expert);
        case 'novice':
          return MapEntry(key, t.novice);
        case 'junior':
          return MapEntry(key, t.junior);
        case 'midLevel':
          return MapEntry(key, t.midLevel);
        case 'senior':
          return MapEntry(key, t.senior);
        case 'proficient':
          return MapEntry(key, t.proficient);
        case 'master':
          return MapEntry(key, t.master);
        case 'lead':
          return MapEntry(key, t.lead);
        case 'sureHereABasicCodeForAnCounterAppUsingDartFlutterFramework':
          return MapEntry(
            key,
            t.sureHereABasicCodeForAnCounterAppUsingDartFlutterFramework,
          );
        case 'documentName':
          return MapEntry(key, t.documentName);
        case 'textToImage':
          return MapEntry(key, t.textToImage);
        case 'imageToImage':
          return MapEntry(key, t.imageToImage);
        case 'describeAnImageYouWantToGenerate':
          return MapEntry(key, t.describeAnImageYouWantToGenerate);
        case 'generate':
          return MapEntry(key, t.generate);
        case 'clickOrDropAnImageHere':
          return MapEntry(key, t.clickOrDropAnImageHere);
        case 'upTo10MB':
          return MapEntry(key, t.upTo10MB);
        case 'today':
          return MapEntry(key, t.today);
        case 'oF':
          return MapEntry(key, t.oF);
        case 'narration':
          return MapEntry(key, t.narration);
        case 'conversational':
          return MapEntry(key, t.conversational);
        case 'formal':
          return MapEntry(key, t.formal);
        case 'casual':
          return MapEntry(key, t.casual);
        case 'excited':
          return MapEntry(key, t.excited);
        case 'calm':
          return MapEntry(key, t.calm);
        case 'serious':
          return MapEntry(key, t.serious);
        case 'friendly':
          return MapEntry(key, t.friendly);
        case 'inspirational':
          return MapEntry(key, t.inspirational);
        case 'persuasive':
          return MapEntry(key, t.persuasive);
        case 'sad':
          return MapEntry(key, t.sad);
        case 'joyful':
          return MapEntry(key, t.joyful);
        case 'neutral':
          return MapEntry(key, t.neutral);
        case 'authoritative':
          return MapEntry(key, t.authoritative);
        case 'warm':
          return MapEntry(key, t.warm);
        case 'playful':
          return MapEntry(key, t.playful);
        case 'dramatic':
          return MapEntry(key, t.dramatic);
        case 'clear':
          return MapEntry(key, t.clear);
        case 'empathetic':
          return MapEntry(key, t.empathetic);
        case 'instructional':
          return MapEntry(key, t.instructional);
        case 'fileName':
          return MapEntry(key, t.fileName);
        case 'enterFileName':
          return MapEntry(key, t.enterFileName);
        case 'language':
          return MapEntry(key, t.language);
        case 'voices':
          return MapEntry(key, t.voices);
        case 'speakingStyle':
          return MapEntry(key, t.speakingStyle);
        case 'pause':
          return MapEntry(key, t.pause);
        case 'selectOne':
          return MapEntry(key, t.selectOne);
        case 'second':
          return MapEntry(key, t.second);
        case 'downloadFiels':
          return MapEntry(key, t.downloadFiels);
        case 'speed':
          return MapEntry(key, t.speed);
        case 'makeUpA5SentenceStoryAboutSharkyAToothBrushingSharkSuperhero':
          return MapEntry(
            key,
            t.makeUpA5SentenceStoryAboutSharkyAToothBrushingSharkSuperhero,
          );
        case 'certainlyToProvideYouWithMoreRelevant':
          return MapEntry(key, t.certainlyToProvideYouWithMoreRelevant);
        case 'subscriptionPlanTitlePremiumMembership':
          return MapEntry(key, t.subscriptionPlanTitlePremiumMembership);
        case 'unlockAWorldOfExclusiveBenefitsWithOurPremiumMembership':
          return MapEntry(
            key,
            t.unlockAWorldOfExclusiveBenefitsWithOurPremiumMembership,
          );
        case 'introductionSection':
          return MapEntry(key, t.introductionSection);
        case 'unlockAWorldOfExclusiveBenefits':
          return MapEntry(key, t.unlockAWorldOfExclusiveBenefits);
        case 'upgradeToOurPremiumMembershipForAnnparalleledExperience':
          return MapEntry(
            key,
            t.upgradeToOurPremiumMembershipForAnnparalleledExperience,
          );
        case 'featuresHighlights':
          return MapEntry(key, t.featuresHighlights);
        case 'pricingInformation':
          return MapEntry(key, t.pricingInformation);
        case 'limitlessAccess':
          return MapEntry(key, t.limitlessAccess);
        case 'chooseThePlanThatSuitsYourNeedsNest':
          return MapEntry(key, t.chooseThePlanThatSuitsYourNeedsNest);
        case 'monthlyPlan999Month':
          return MapEntry(key, t.monthlyPlan999Month);
        case 'annualPlanSave209588year':
          return MapEntry(key, t.annualPlanSave209588year);
        case 'callToActionSection':
          return MapEntry(key, t.callToActionSection);
        case 'readyToElevateYourExperience':
          return MapEntry(key, t.readyToElevateYourExperience);
        case 'subscribeNowButton':
          return MapEntry(key, t.subscribeNowButton);
        case 'guaranteeSection':
          return MapEntry(key, t.guaranteeSection);
        case 'riskFreeSatisfactionGuaranteed':
          return MapEntry(key, t.riskFreeSatisfactionGuaranteed);
        case 'fAQSection':
          return MapEntry(key, t.fAQSection);
        case 'haveQuestionsWeGotAnswers':
          return MapEntry(key, t.haveQuestionsWeGotAnswers);
        case 'howDoICancelMySubscription':
          return MapEntry(key, t.howDoICancelMySubscription);
        case 'canISwitchPlansAtAnyTime':
          return MapEntry(key, t.canISwitchPlansAtAnyTime);
        case 'isMyPaymentInformationSecure':
          return MapEntry(key, t.isMyPaymentInformationSecure);
        case 'footerSection':
          return MapEntry(key, t.footerSection);
        case 'joinYourCompanyPremiumAndExperience':
          return MapEntry(key, t.joinYourCompanyPremiumAndExperience);
        case 'notSatisfiedWeOfferA30DayMoneyBackGuaranteeNoQuestionsAsked':
          return MapEntry(
            key,
            t.notSatisfiedWeOfferA30DayMoneyBackGuaranteeNoQuestionsAsked,
          );
        case 'feelFreeToCustomizeThisCopyAccordingToYourBrandVoice':
          return MapEntry(
            key,
            t.feelFreeToCustomizeThisCopyAccordingToYourBrandVoice,
          );
        case 'template':
          return MapEntry(key, t.template);
        case 'maximumLength':
          return MapEntry(key, t.maximumLength);
        case 'numberOfResults':
          return MapEntry(key, t.numberOfResults);
        case 'creativity':
          return MapEntry(key, t.creativity);
        case 'toneOfVoice':
          return MapEntry(key, t.toneOfVoice);
        case 'blogWritingSuggestion':
          return MapEntry(key, t.blogWritingSuggestion);
        case 'writeAText':
          return MapEntry(key, t.writeAText);
        case 'compareBusinessStrategies':
          return MapEntry(key, t.compareBusinessStrategies);
        case 'createAPersonalContentForMe':
          return MapEntry(key, t.createAPersonalContentForMe);
        case 'filter':
          return MapEntry(key, t.filter);
        case 'productDescriptions':
          return MapEntry(key, t.productDescriptions);
        case 'socialMediaCaptions':
          return MapEntry(key, t.socialMediaCaptions);
        case 'emailNewsletters':
          return MapEntry(key, t.emailNewsletters);
        case 'sEOMetaDescriptions':
          return MapEntry(key, t.sEOMetaDescriptions);
        case 'adCopy':
          return MapEntry(key, t.adCopy);
        case 'landingPageCopy':
          return MapEntry(key, t.landingPageCopy);
        case 'pressReleases':
          return MapEntry(key, t.pressReleases);
        case 'whitepapers':
          return MapEntry(key, t.whitepapers);
        case 'caseStudies':
          return MapEntry(key, t.caseStudies);
        case 'videoScripts':
          return MapEntry(key, t.videoScripts);
        case 'ecommerceProductListings':
          return MapEntry(key, t.ecommerceProductListings);
        case 'websiteContent':
          return MapEntry(key, t.websiteContent);
        case 'technicalDocumentation':
          return MapEntry(key, t.technicalDocumentation);
        case 'creativeWritingShortStories':
          return MapEntry(key, t.creativeWritingShortStories);
        case 'brandStorytelling':
          return MapEntry(key, t.brandStorytelling);
        case 'resumeandCoverLetters':
          return MapEntry(key, t.resumeandCoverLetters);
        case 'appStoreDescriptions':
          return MapEntry(key, t.appStoreDescriptions);
        case 'ebookWriting':
          return MapEntry(key, t.ebookWriting);
        case 'customerTestimonials':
          return MapEntry(key, t.customerTestimonials);
        case 'salesCopy':
          return MapEntry(key, t.salesCopy);
        case 'howtoGuides':
          return MapEntry(key, t.howtoGuides);
        case 'fAQsWriting':
          return MapEntry(key, t.fAQsWriting);
        case 'jobDescriptions':
          return MapEntry(key, t.jobDescriptions);
        case 'businessProposals':
          return MapEntry(key, t.businessProposals);
        case 'coldEmailOutreach':
          return MapEntry(key, t.coldEmailOutreach);
        case 'speechWriting':
          return MapEntry(key, t.speechWriting);
        case 'interviewQuestionWriting':
          return MapEntry(key, t.interviewQuestionWriting);
        case 'reviewResponses':
          return MapEntry(key, t.reviewResponses);
        case 'eventInvitations':
          return MapEntry(key, t.eventInvitations);
        case 'witty':
          return MapEntry(key, t.witty);
        case 'imaginative':
          return MapEntry(key, t.imaginative);
        case 'direct':
          return MapEntry(key, t.direct);
        case 'energetic':
          return MapEntry(key, t.energetic);
        case 'reflective':
          return MapEntry(key, t.reflective);
        case 'adventurous':
          return MapEntry(key, t.adventurous);
        case 'quirky':
          return MapEntry(key, t.quirky);
        case 'professional':
          return MapEntry(key, t.professional);
        case 'confident':
          return MapEntry(key, t.confident);
        case 'respectful':
          return MapEntry(key, t.respectful);
        case 'bold':
          return MapEntry(key, t.bold);
        case 'caring':
          return MapEntry(key, t.caring);
        case 'optimistic':
          return MapEntry(key, t.optimistic);
        case 'sophisticated':
          return MapEntry(key, t.sophisticated);
        case 'humorous':
          return MapEntry(key, t.humorous);
        case 'thoughtful':
          return MapEntry(key, t.thoughtful);
        case 'keywordsSeparateWithComma':
          return MapEntry(key, t.keywordsSeparateWithComma);
        case 'egMaanthemeAcnoo':
          return MapEntry(key, t.egMaanthemeAcnoo);
        case 'dashboard':
          return MapEntry(key, t.dashboard);
        case 'openAIAdmin':
          return MapEntry(key, t.openAIAdmin);
        case 'eRPAdmin':
          return MapEntry(key, t.eRPAdmin);
        case 'pOSAdmin':
          return MapEntry(key, t.pOSAdmin);
        case 'earningAdmin':
          return MapEntry(key, t.earningAdmin);
        case 'sMSAdmin':
          return MapEntry(key, t.sMSAdmin);
        case 'influencerAdmin':
          return MapEntry(key, t.influencerAdmin);
        case 'hRMAdmin':
          return MapEntry(key, t.hRMAdmin);
        case 'newsAdmin':
          return MapEntry(key, t.newsAdmin);
        case 'eCommerceAdmin':
          return MapEntry(key, t.eCommerceAdmin);
        case 'widgets':
          return MapEntry(key, t.widgets);
        case 'general':
          return MapEntry(key, t.general);
        case 'chart':
          return MapEntry(key, t.chart);
        case 'application':
          return MapEntry(key, t.application);
        case 'chat':
          return MapEntry(key, t.chat);
        case 'projects':
          return MapEntry(key, t.projects);
        case 'kanban':
          return MapEntry(key, t.kanban);
        case 'openAI':
          return MapEntry(key, t.openAI);
        case 'aIWriter':
          return MapEntry(key, t.aIWriter);
        case 'aIImage':
          return MapEntry(key, t.aIImage);
        case 'aIChat':
          return MapEntry(key, t.aIChat);
        case 'aICode':
          return MapEntry(key, t.aICode);
        case 'aIVoiceover':
          return MapEntry(key, t.aIVoiceover);
        case 'users':
          return MapEntry(key, t.users);
        case 'usersList':
          return MapEntry(key, t.usersList);
        case 'usersGrid':
          return MapEntry(key, t.usersGrid);
        case 'tablesForms':
          return MapEntry(key, t.tablesForms);
        case 'tables':
          return MapEntry(key, t.tables);
        case 'basicTable':
          return MapEntry(key, t.basicTable);
        case 'dataTable':
          return MapEntry(key, t.dataTable);
        case 'forms':
          return MapEntry(key, t.forms);
        case 'basicForms':
          return MapEntry(key, t.basicForms);
        case 'formSelect':
          return MapEntry(key, t.formSelect);
        case 'validation':
          return MapEntry(key, t.validation);
        case 'components':
          return MapEntry(key, t.components);
        case 'buttons':
          return MapEntry(key, t.buttons);
        case 'colors':
          return MapEntry(key, t.colors);
        case 'alert':
          return MapEntry(key, t.alert);
        case 'typography':
          return MapEntry(key, t.typography);
        case 'cards':
          return MapEntry(key, t.cards);
        case 'avatars':
          return MapEntry(key, t.avatars);
        case 'dragDrop':
          return MapEntry(key, t.dragDrop);
        case 'pages':
          return MapEntry(key, t.pages);
        case 'authentication':
          return MapEntry(key, t.authentication);
        case 'gallery':
          return MapEntry(key, t.gallery);
        case 'maps':
          return MapEntry(key, t.maps);
        case 'pricing':
          return MapEntry(key, t.pricing);
        case 'fAQs':
          return MapEntry(key, t.fAQs);
        case 'tabsPills':
          return MapEntry(key, t.tabsPills);
        case 'error':
          return MapEntry(key, t.error);
        case 'privacyPolicy':
          return MapEntry(key, t.privacyPolicy);
        case 'termsConditions':
          return MapEntry(key, t.termsConditions);
        case 'introduction':
          return MapEntry(key, t.introduction);
        case 'informationCollection':
          return MapEntry(key, t.informationCollection);
        case 'useofInformation':
          return MapEntry(key, t.useofInformation);
        case 'dataSharing':
          return MapEntry(key, t.dataSharing);
        case 'dataSecurity':
          return MapEntry(key, t.dataSecurity);
        case 'yourRights':
          return MapEntry(key, t.yourRights);
        case 'changestoThisPolicy':
          return MapEntry(key, t.changestoThisPolicy);
        case 'contactUs':
          return MapEntry(key, t.contactUs);
        case 'ourPrivacyPolicyProvidesAComprehensive':
          return MapEntry(key, t.ourPrivacyPolicyProvidesAComprehensive);
        case 'weGatherARangeOfInformationToEnhanceYourExperience':
          return MapEntry(
            key,
            t.weGatherARangeOfInformationToEnhanceYourExperience,
          );
        case 'theInformationWeCollectHelpsUsEnhanceOurServices':
          return MapEntry(
            key,
            t.theInformationWeCollectHelpsUsEnhanceOurServices,
          );
        case 'weDoNotSellOrRentYourPersonalInformationToThirdParties':
          return MapEntry(
            key,
            t.weDoNotSellOrRentYourPersonalInformationToThirdParties,
          );
        case 'weImplementAppropriateSecurityMeasures':
          return MapEntry(key, t.weImplementAppropriateSecurityMeasures);
        case 'youHaveTheRightToAccessCorrect':
          return MapEntry(key, t.youHaveTheRightToAccessCorrect);
        case 'weMayUpdateThisPrivacyPolicyFromTimeToTime':
          return MapEntry(key, t.weMayUpdateThisPrivacyPolicyFromTimeToTime);
        case 'ifYouHaveAnyQuestionsOrConcernsAboutOurPrivacyPolicy':
          return MapEntry(
            key,
            t.ifYouHaveAnyQuestionsOrConcernsAboutOurPrivacyPolicy,
          );
        case 'subMenusCannotBeNullOrEmptyIfTheItemTypeIsSubmenu':
          return MapEntry(
            key,
            t.subMenusCannotBeNullOrEmptyIfTheItemTypeIsSubmenu,
          );
        case 'complete':
          return MapEntry(key, t.complete);
        case 'home':
          return MapEntry(key, t.home);
        case 'agreementToTerms':
          return MapEntry(key, t.agreementToTerms);
        case 'userResponsibilities':
          return MapEntry(key, t.userResponsibilities);
        case 'prohibitedActivities':
          return MapEntry(key, t.prohibitedActivities);
        case 'serviceModifications':
          return MapEntry(key, t.serviceModifications);
        case 'limitationOfLiability':
          return MapEntry(key, t.limitationOfLiability);
        case 'governingLaw':
          return MapEntry(key, t.governingLaw);
        case 'changesToTheseTerms':
          return MapEntry(key, t.changesToTheseTerms);
        case 'contactInformation':
          return MapEntry(key, t.contactInformation);
        case 'byAccessingOrUsingOurWebsiteAndServices':
          return MapEntry(key, t.byAccessingOrUsingOurWebsiteAndServices);
        case 'asAUserOfOurPlatform':
          return MapEntry(key, t.asAUserOfOurPlatform);
        case 'youAreProhibitedFromEngagingInActivities':
          return MapEntry(key, t.youAreProhibitedFromEngagingInActivities);
        case 'weReserveTheRightToModify':
          return MapEntry(key, t.weReserveTheRightToModify);
        case 'ourPlatformAndServicesAreProvided':
          return MapEntry(key, t.ourPlatformAndServicesAreProvided);
        case 'theseTermsAndConditionsAreGoverned':
          return MapEntry(key, t.theseTermsAndConditionsAreGoverned);
        case 'weMayUpdateTheseTermsAndConditionsPeriodically':
          return MapEntry(
            key,
            t.weMayUpdateTheseTermsAndConditionsPeriodically,
          );
        case 'ifYouHaveAnyQuestionsOrConcernsRegardingTheseTermsAndConditions':
          return MapEntry(
            key,
            t.ifYouHaveAnyQuestionsOrConcernsRegardingTheseTermsAndConditions,
          );
        case 'viewed':
          return MapEntry(key, t.viewed);
        case 'defaultLineChart':
          return MapEntry(key, t.defaultLineChart);
        case 'multipleLineChart':
          return MapEntry(key, t.multipleLineChart);
        case 'areaChart':
          return MapEntry(key, t.areaChart);
        case 'transparentChart':
          return MapEntry(key, t.transparentChart);
        case 'mingguanChart':
          return MapEntry(key, t.mingguanChart);
        case 'pieChart':
          return MapEntry(key, t.pieChart);
        case 'bitcoin':
          return MapEntry(key, t.bitcoin);
        case 'solana':
          return MapEntry(key, t.solana);
        case 'ethereum':
          return MapEntry(key, t.ethereum);
        case 'bTC':
          return MapEntry(key, t.bTC);
        case 'sOL':
          return MapEntry(key, t.sOL);
        case 'eTH':
          return MapEntry(key, t.eTH);
        case 'visitsByDay':
          return MapEntry(key, t.visitsByDay);
        case 'thisMonthLeaveRequest':
          return MapEntry(key, t.thisMonthLeaveRequest);
        case 'sinceLastMonthLeaveRequest':
          return MapEntry(key, t.sinceLastMonthLeaveRequest);
        case 'thisMonthSalary':
          return MapEntry(key, t.thisMonthSalary);
        case 'sinceLastMonthSalary':
          return MapEntry(key, t.sinceLastMonthSalary);
        case 'totalEmployees':
          return MapEntry(key, t.totalEmployees);
        case 'todayPresent':
          return MapEntry(key, t.todayPresent);
        case 'thisMonthExpenses':
          return MapEntry(key, t.thisMonthExpenses);
        case 'sinceLastMonthExpenses':
          return MapEntry(key, t.sinceLastMonthExpenses);
        case 'arleneMcCoy':
          return MapEntry(key, t.arleneMcCoy);
        case 'eventPlanner':
          return MapEntry(key, t.eventPlanner);
        case 'ralphEdwards':
          return MapEntry(key, t.ralphEdwards);
        case 'languageTutor':
          return MapEntry(key, t.languageTutor);
        case 'fitnessTrainer':
          return MapEntry(key, t.fitnessTrainer);
        case 'facebook':
          return MapEntry(key, t.facebook);
        case 'twitter':
          return MapEntry(key, t.twitter);
        case 'instagram':
          return MapEntry(key, t.instagram);
        case 'youtube':
          return MapEntry(key, t.youtube);
        case 'linkedin':
          return MapEntry(key, t.linkedin);
        case 'pinterest':
          return MapEntry(key, t.pinterest);
        case 'orderStatus':
          return MapEntry(key, t.orderStatus);
        case 'salesOverview':
          return MapEntry(key, t.salesOverview);
        case 'totalProducts':
          return MapEntry(key, t.totalProducts);
        case 'totalStores':
          return MapEntry(key, t.totalStores);
        case 'totalDeliveryBoy':
          return MapEntry(key, t.totalDeliveryBoy);
        case 'totalRevenue':
          return MapEntry(key, t.totalRevenue);
        case 'totalUsers':
          return MapEntry(key, t.totalUsers);
        case 'totalIncome':
          return MapEntry(key, t.totalIncome);
        case 'totalSubscription':
          return MapEntry(key, t.totalSubscription);
        case 'totalExpense':
          return MapEntry(key, t.totalExpense);
        case 'purchase':
          return MapEntry(key, t.purchase);
        case 'uPIPay':
          return MapEntry(key, t.uPIPay);
        case 'sports':
          return MapEntry(key, t.sports);
        case 'courses':
          return MapEntry(key, t.courses);
        case 'music':
          return MapEntry(key, t.music);
        case 'science':
          return MapEntry(key, t.science);
        case 'quizName':
          return MapEntry(key, t.quizName);
        case 'rewardPoint':
          return MapEntry(key, t.rewardPoint);
        case 'createdAt':
          return MapEntry(key, t.createdAt);
        case 'withdraw':
          return MapEntry(key, t.withdraw);
        case 'sMSDeliveried':
          return MapEntry(key, t.sMSDeliveried);
        case 'sMSFailed':
          return MapEntry(key, t.sMSFailed);
        case 'balance':
          return MapEntry(key, t.balance);
        case 'sMSHistoryStatistics':
          return MapEntry(key, t.sMSHistoryStatistics);
        case 'sMSReport':
          return MapEntry(key, t.sMSReport);
        case 'topUsers':
          return MapEntry(key, t.topUsers);
        case 'latestQuiz':
          return MapEntry(key, t.latestQuiz);
        case 'totalQuizCategory':
          return MapEntry(key, t.totalQuizCategory);
        case 'totalQuizzes':
          return MapEntry(key, t.totalQuizzes);
        case 'totalQuestions':
          return MapEntry(key, t.totalQuestions);
        case 'totalWithdraw':
          return MapEntry(key, t.totalWithdraw);
        case 'pendingWithdraw':
          return MapEntry(key, t.pendingWithdraw);
        case 'approvedWithdraw':
          return MapEntry(key, t.approvedWithdraw);
        case 'rejectedWithdraw':
          return MapEntry(key, t.rejectedWithdraw);
        case 'totalUser':
          return MapEntry(key, t.totalUser);
        case 'totalCurrency':
          return MapEntry(key, t.totalCurrency);
        case 'totalRewards':
          return MapEntry(key, t.totalRewards);
        case 'formValidation':
          return MapEntry(key, t.formValidation);
        case 'alerts':
          return MapEntry(key, t.alerts);
        case 'loremIpsumIsSimplyDummyTextOfThePrinting':
          return MapEntry(key, t.loremIpsumIsSimplyDummyTextOfThePrinting);
        case 'black':
          return MapEntry(key, t.black);
        case 'white':
          return MapEntry(key, t.white);
        case 'bases':
          return MapEntry(key, t.bases);
        case 'family':
          return MapEntry(key, t.family);
        case 'promotions':
          return MapEntry(key, t.promotions);
        case 'social':
          return MapEntry(key, t.social);
        case 'thisIsImageTitle':
          return MapEntry(key, t.thisIsImageTitle);
        case 'uIDesign':
          return MapEntry(key, t.uIDesign);
        case 'inReview':
          return MapEntry(key, t.inReview);
        case 'businessDashboard':
          return MapEntry(key, t.businessDashboard);
        case 'loremIpsumDolorSitAmet':
          return MapEntry(key, t.loremIpsumDolorSitAmet);
        case 'molestiaeQuiaUtCumqueSitNihilIpsamRepellendus':
          return MapEntry(key, t.molestiaeQuiaUtCumqueSitNihilIpsamRepellendus);
        case 'standard':
          return MapEntry(key, t.standard);
        case 'proBusiness':
          return MapEntry(key, t.proBusiness);
        case 'enterprise':
          return MapEntry(key, t.enterprise);
        case 'forTheProfessionals':
          return MapEntry(key, t.forTheProfessionals);
        case 'user':
          return MapEntry(key, t.user);
        case 'assetDelivery':
          return MapEntry(key, t.assetDelivery);
        case 'unlimitedBandwidth':
          return MapEntry(key, t.unlimitedBandwidth);
        case 'supportSystem':
          return MapEntry(key, t.supportSystem);
        case 'messageAllOperator':
          return MapEntry(key, t.messageAllOperator);
        case 'freebie':
          return MapEntry(key, t.freebie);
        case 'idealForIndividualsWhoNeedQuickAccessToBasicFeatures':
          return MapEntry(
            key,
            t.idealForIndividualsWhoNeedQuickAccessToBasicFeatures,
          );
        case 'pNGSVGGraphics':
          return MapEntry(key, t.pNGSVGGraphics);
        case 'accessTo100MillionStockImages':
          return MapEntry(key, t.accessTo100MillionStockImages);
        case 'uploadCustomIconsAndFonts':
          return MapEntry(key, t.uploadCustomIconsAndFonts);
        case 'unlimitedSharing':
          return MapEntry(key, t.unlimitedSharing);
        case 'uploadGraphicsVideoInUpTo4k':
          return MapEntry(key, t.uploadGraphicsVideoInUpTo4k);
        case 'unlimitedProjects':
          return MapEntry(key, t.unlimitedProjects);
        case 'instantAccessToOurDesignSystem':
          return MapEntry(key, t.instantAccessToOurDesignSystem);
        case 'createTeamsToCollaborateOnDesigns':
          return MapEntry(key, t.createTeamsToCollaborateOnDesigns);
        case 'totalEarning':
          return MapEntry(key, t.totalEarning);
        case 'wordGenerated':
          return MapEntry(key, t.wordGenerated);
        case 'first':
          return MapEntry(key, t.first);
        case 'third':
          return MapEntry(key, t.third);
        case 'fourth':
          return MapEntry(key, t.fourth);
        case 'startChat':
          return MapEntry(key, t.startChat);
        case 'orders':
          return MapEntry(key, t.orders);
        case 'products':
          return MapEntry(key, t.products);
        case 'followers':
          return MapEntry(key, t.followers);
        case 'following':
          return MapEntry(key, t.following);
        case 'choosePlan':
          return MapEntry(key, t.choosePlan);
        case 'getStartedNow':
          return MapEntry(key, t.getStartedNow);
        case 'dailyUses':
          return MapEntry(key, t.dailyUses);
        case 'todayIncome':
          return MapEntry(key, t.todayIncome);
        case 'monthly':
          return MapEntry(key, t.monthly);
        case 'yearly':
          return MapEntry(key, t.yearly);
        case 'articles':
          return MapEntry(key, t.articles);
        case 'codes':
          return MapEntry(key, t.codes);
        case 'images':
          return MapEntry(key, t.images);
        case 'chats':
          return MapEntry(key, t.chats);
        case 'voiceover':
          return MapEntry(key, t.voiceover);
        case 'readMore':
          return MapEntry(key, t.readMore);
        case 'getStarted':
          return MapEntry(key, t.getStarted);
        case 'helloShaidulIslam':
          return MapEntry(key, t.helloShaidulIslam);
        case 'yourAccountIsCurrently':
          return MapEntry(key, t.yourAccountIsCurrently);
        case 'freePlan':
          return MapEntry(key, t.freePlan);
        case 'upgradePlan':
          return MapEntry(key, t.upgradePlan);
        case 'buyCredits':
          return MapEntry(key, t.buyCredits);
        case 'topCustomers':
          return MapEntry(key, t.topCustomers);
        case 'topSellingItems':
          return MapEntry(key, t.topSellingItems);
        case 'earningReports':
          return MapEntry(key, t.earningReports);
        case 'topBrands':
          return MapEntry(key, t.topBrands);
        case 'userStatistics':
          return MapEntry(key, t.userStatistics);
        case 'topVendors':
          return MapEntry(key, t.topVendors);
        case 'topDeliveryMan':
          return MapEntry(key, t.topDeliveryMan);
        case 'processing':
          return MapEntry(key, t.processing);
        case 'cancelled':
          return MapEntry(key, t.cancelled);
        case 'shipped':
          return MapEntry(key, t.shipped);
        case 'outOfDelivery':
          return MapEntry(key, t.outOfDelivery);
        case 'delivered':
          return MapEntry(key, t.delivered);
        case 'samsung':
          return MapEntry(key, t.samsung);
        case 'fashion':
          return MapEntry(key, t.fashion);
        case 'generalMotors':
          return MapEntry(key, t.generalMotors);
        case 'foods':
          return MapEntry(key, t.foods);
        case 'revenue':
          return MapEntry(key, t.revenue);
        case 'subTitle':
          return MapEntry(key, t.subTitle);
        case 'bodyText':
          return MapEntry(key, t.bodyText);
        case 'smallText':
          return MapEntry(key, t.smallText);
        case 'thisIsTitleLoremIpsumIsSimplyDummy':
          return MapEntry(key, t.thisIsTitleLoremIpsumIsSimplyDummy);
        case 'customer':
          return MapEntry(key, t.customer);
        case 'store':
          return MapEntry(key, t.store);
        case 'deliveryMan':
          return MapEntry(key, t.deliveryMan);
        case 'plan':
          return MapEntry(key, t.plan);
        case 'free':
          return MapEntry(key, t.free);
        case 'review':
          return MapEntry(key, t.review);
        case 'orderCompleted':
          return MapEntry(key, t.orderCompleted);
        case 'latestRegistered':
          return MapEntry(key, t.latestRegistered);
        case 'year':
          return MapEntry(key, t.year);
        case 'loremIpsumIsSimplyDummyTextOfThePrintingAndTypesettingIndustry2':
          return MapEntry(
            key,
            t.loremIpsumIsSimplyDummyTextOfThePrintingAndTypesettingIndustry2,
          );
        case 'powerful':
          return MapEntry(key, t.powerful);
        case 'features':
          return MapEntry(key, t.features);
        case 'pricingPlan':
          return MapEntry(key, t.pricingPlan);
        case 'customersGrowth':
          return MapEntry(key, t.customersGrowth);
        case 'totalCustomers':
          return MapEntry(key, t.totalCustomers);
        case 'thisMonthCustomers':
          return MapEntry(key, t.thisMonthCustomers);
        case 'thisMonthSMS':
          return MapEntry(key, t.thisMonthSMS);
        case 'todaySendSMS':
          return MapEntry(key, t.todaySendSMS);
        case 'totalPlan':
          return MapEntry(key, t.totalPlan);
        case 'basicStandardEnterprise':
          return MapEntry(key, t.basicStandardEnterprise);
        case 'thisMonthIncome':
          return MapEntry(key, t.thisMonthIncome);
        case 'jan':
          return MapEntry(key, t.jan);
        case 'feb':
          return MapEntry(key, t.feb);
        case 'mar':
          return MapEntry(key, t.mar);
        case 'apr':
          return MapEntry(key, t.apr);
        case 'may':
          return MapEntry(key, t.may);
        case 'jun':
          return MapEntry(key, t.jun);
        case 'jul':
          return MapEntry(key, t.jul);
        case 'aug':
          return MapEntry(key, t.aug);
        case 'sept':
          return MapEntry(key, t.sept);
        case 'oct':
          return MapEntry(key, t.oct);
        case 'nov':
          return MapEntry(key, t.nov);
        case 'dec':
          return MapEntry(key, t.dec);
        case 'outgoing':
          return MapEntry(key, t.outgoing);
        case 'incoming':
          return MapEntry(key, t.incoming);
        case 'joinDate':
          return MapEntry(key, t.joinDate);
        case 'orderDone':
          return MapEntry(key, t.orderDone);
        case 'recentRegisteredClient':
          return MapEntry(key, t.recentRegisteredClient);
        case 'incomeReport':
          return MapEntry(key, t.incomeReport);
        case 'recentRegisteredInfluencer':
          return MapEntry(key, t.recentRegisteredInfluencer);
        case 'withdrawReport':
          return MapEntry(key, t.withdrawReport);
        case 'totalClients':
          return MapEntry(key, t.totalClients);
        case 'activeClients':
          return MapEntry(key, t.activeClients);
        case 'totalInfluencer':
          return MapEntry(key, t.totalInfluencer);
        case 'activeInfluencer':
          return MapEntry(key, t.activeInfluencer);
        case 'totalServices':
          return MapEntry(key, t.totalServices);
        case 'totalStaff':
          return MapEntry(key, t.totalStaff);
        case 'present':
          return MapEntry(key, t.present);
        case 'absent':
          return MapEntry(key, t.absent);
        case 'lateUser':
          return MapEntry(key, t.lateUser);
        case 'employeeId':
          return MapEntry(key, t.employeeId);
        case 'employeeName':
          return MapEntry(key, t.employeeName);
        case 'from':
          return MapEntry(key, t.from);
        case 'leaveDuration':
          return MapEntry(key, t.leaveDuration);
        case 'rejected':
          return MapEntry(key, t.rejected);
        case 'leslieAlexander':
          return MapEntry(key, t.leslieAlexander);
        case 'courtneyHenry':
          return MapEntry(key, t.courtneyHenry);
        case 'kristinWatson':
          return MapEntry(key, t.kristinWatson);
        case 'total':
          return MapEntry(key, t.total);
        case 'salaryReport':
          return MapEntry(key, t.salaryReport);
        case 'leaveRequest':
          return MapEntry(key, t.leaveRequest);
        case 'upcomingBirthday':
          return MapEntry(key, t.upcomingBirthday);
        case 'birthdayDate':
          return MapEntry(key, t.birthdayDate);
        case 'exitEmployee':
          return MapEntry(key, t.exitEmployee);
        case 'employeeAttendance':
          return MapEntry(key, t.employeeAttendance);
        case 'freeUsers':
          return MapEntry(key, t.freeUsers);
        case 'subscribed':
          return MapEntry(key, t.subscribed);
        case 'subscribe':
          return MapEntry(key, t.subscribe);
        case 'userStatistic':
          return MapEntry(key, t.userStatistic);
        case 'categoryWiseNews':
          return MapEntry(key, t.categoryWiseNews);
        case 'mostViewedNews':
          return MapEntry(key, t.mostViewedNews);
        case 'latestComments':
          return MapEntry(key, t.latestComments);
        case 'newsReaders':
          return MapEntry(key, t.newsReaders);
        case 'category':
          return MapEntry(key, t.category);
        case 'featuredSection':
          return MapEntry(key, t.featuredSection);
        case 'adSpaces':
          return MapEntry(key, t.adSpaces);
        case 'breakingNews':
          return MapEntry(key, t.breakingNews);
        case 'publishedNews':
          return MapEntry(key, t.publishedNews);
        case 'draftNews':
          return MapEntry(key, t.draftNews);
        case 'totalBlogs':
          return MapEntry(key, t.totalBlogs);
        case 'uSInflationDeceleratingInBoostToEconomy':
          return MapEntry(key, t.uSInflationDeceleratingInBoostToEconomy);
        case 'politics':
          return MapEntry(key, t.politics);
        case 'familyS':
          return MapEntry(key, t.familyS);
        case 'health':
          return MapEntry(key, t.health);
        case 'travels':
          return MapEntry(key, t.travels);
        case 'religion':
          return MapEntry(key, t.religion);
        case 'minsAgo':
          return MapEntry(key, t.minsAgo);
        case 'greatNews':
          return MapEntry(key, t.greatNews);
        case 'eCommerce':
          return MapEntry(key, t.eCommerce);
        case 'orderSummary':
          return MapEntry(key, t.orderSummary);
        case 'couponCode':
          return MapEntry(key, t.couponCode);
        case 'enterCouponCode':
          return MapEntry(key, t.enterCouponCode);
        case 'proceedToCheckout':
          return MapEntry(key, t.proceedToCheckout);
        case 'subtotal':
          return MapEntry(key, t.subtotal);
        case 'discount':
          return MapEntry(key, t.discount);
        case 'shippingCharge':
          return MapEntry(key, t.shippingCharge);
        case 'tax':
          return MapEntry(key, t.tax);
        case 'price':
          return MapEntry(key, t.price);
        case 'quantity':
          return MapEntry(key, t.quantity);
        case 'enterFirstName':
          return MapEntry(key, t.enterFirstName);
        case 'enterLastName':
          return MapEntry(key, t.enterLastName);
        case 'companyName':
          return MapEntry(key, t.companyName);
        case 'enterCompanyName':
          return MapEntry(key, t.enterCompanyName);
        case 'countryRegion':
          return MapEntry(key, t.countryRegion);
        case 'streetAddress':
          return MapEntry(key, t.streetAddress);
        case 'enterStreetAddress':
          return MapEntry(key, t.enterStreetAddress);
        case 'townCity':
          return MapEntry(key, t.townCity);
        case 'enterTownCity':
          return MapEntry(key, t.enterTownCity);
        case 'enterState':
          return MapEntry(key, t.enterState);
        case 'zIPCode':
          return MapEntry(key, t.zIPCode);
        case 'enterZIPCode':
          return MapEntry(key, t.enterZIPCode);
        case 'enterPhone':
          return MapEntry(key, t.enterPhone);
        case 'emailAddress':
          return MapEntry(key, t.emailAddress);
        case 'additionalInformation':
          return MapEntry(key, t.additionalInformation);
        case 'orderNotes':
          return MapEntry(key, t.orderNotes);
        case 'notesAboutYourOrder':
          return MapEntry(key, t.notesAboutYourOrder);
        case 'yourOrders':
          return MapEntry(key, t.yourOrders);
        case 'product':
          return MapEntry(key, t.product);
        case 'placeOrder':
          return MapEntry(key, t.placeOrder);
        case 'clickHereToEnterYourCode':
          return MapEntry(key, t.clickHereToEnterYourCode);
        case 'haveACoupon':
          return MapEntry(key, t.haveACoupon);
        case 'addToCart':
          return MapEntry(key, t.addToCart);
        case 'reviews':
          return MapEntry(key, t.reviews);
        case 'relatedProducts':
          return MapEntry(key, t.relatedProducts);
        case 'savannahNguyen':
          return MapEntry(key, t.savannahNguyen);
        case 'inStock':
          return MapEntry(key, t.inStock);
        case 'categories':
          return MapEntry(key, t.categories);
        case 'priceRange':
          return MapEntry(key, t.priceRange);
        case 'productStatus':
          return MapEntry(key, t.productStatus);
        case 'onSale':
          return MapEntry(key, t.onSale);
        case 'starRating':
          return MapEntry(key, t.starRating);
        case 'allCategory':
          return MapEntry(key, t.allCategory);
        case 'womanDress':
          return MapEntry(key, t.womanDress);
        case 'tShirts':
          return MapEntry(key, t.tShirts);
        case 'shoes':
          return MapEntry(key, t.shoes);
        case 'sunGlass':
          return MapEntry(key, t.sunGlass);
        case 'womanBag':
          return MapEntry(key, t.womanBag);
        case 'freshFood':
          return MapEntry(key, t.freshFood);
        case 'smartPhone':
          return MapEntry(key, t.smartPhone);
        case 'computer':
          return MapEntry(key, t.computer);
        case 'sortByPopularity':
          return MapEntry(key, t.sortByPopularity);
        case 'sortByNewestFirst':
          return MapEntry(key, t.sortByNewestFirst);
        case 'sortByOldestFirst':
          return MapEntry(key, t.sortByOldestFirst);
        case 'sortByPriceLowToHigh':
          return MapEntry(key, t.sortByPriceLowToHigh);
        case 'sortByPriceHighToLow':
          return MapEntry(key, t.sortByPriceHighToLow);
        case 'sortByBestRating':
          return MapEntry(key, t.sortByBestRating);
        case 'sortByMostReviewed':
          return MapEntry(key, t.sortByMostReviewed);
        case 'sortByFeatured':
          return MapEntry(key, t.sortByFeatured);
        case 'sortByRelevance':
          return MapEntry(key, t.sortByRelevance);
        case 'sortByDiscount':
          return MapEntry(key, t.sortByDiscount);
        case 'sortByAlphabeticalAZ':
          return MapEntry(key, t.sortByAlphabeticalAZ);
        case 'sortByAlphabeticalZA':
          return MapEntry(key, t.sortByAlphabeticalZA);
        case 'sortByClosestLocation':
          return MapEntry(key, t.sortByClosestLocation);
        case 'sortByTrending':
          return MapEntry(key, t.sortByTrending);
        case 'sortByMostLiked':
          return MapEntry(key, t.sortByMostLiked);
        case 'productList':
          return MapEntry(key, t.productList);
        case 'productDetails':
          return MapEntry(key, t.productDetails);
        case 'cart':
          return MapEntry(key, t.cart);
        case 'checkout':
          return MapEntry(key, t.checkout);
        case 'pOSInventory':
          return MapEntry(key, t.pOSInventory);
        case 'pOSSale':
          return MapEntry(key, t.pOSSale);
        case 'saleList':
          return MapEntry(key, t.saleList);
        case 'purchaseList':
          return MapEntry(key, t.purchaseList);
        case 'addPurchase':
          return MapEntry(key, t.addPurchase);
        case 'code':
          return MapEntry(key, t.code);
        case 'brand':
          return MapEntry(key, t.brand);
        case 'unit':
          return MapEntry(key, t.unit);
        case 'qty':
          return MapEntry(key, t.qty);
        case 'purchasePrice':
          return MapEntry(key, t.purchasePrice);
        case 'salesPrice':
          return MapEntry(key, t.salesPrice);
        case 'subTotal':
          return MapEntry(key, t.subTotal);
        case 'shipping':
          return MapEntry(key, t.shipping);
        case 'totalAmount':
          return MapEntry(key, t.totalAmount);
        case 'receiveAmount':
          return MapEntry(key, t.receiveAmount);
        case 'ex25':
          return MapEntry(key, t.ex25);
        case 'changeAmount':
          return MapEntry(key, t.changeAmount);
        case 'dueAmount':
          return MapEntry(key, t.dueAmount);
        case 'exOutOfLimit':
          return MapEntry(key, t.exOutOfLimit);
        case 'transactionID':
          return MapEntry(key, t.transactionID);
        case 'exHF5gJ7':
          return MapEntry(key, t.exHF5gJ7);
        case 'searchProductByName':
          return MapEntry(key, t.searchProductByName);
        case 'selectASupplier':
          return MapEntry(key, t.selectASupplier);
        case 'supplier':
          return MapEntry(key, t.supplier);
        case 'watch':
          return MapEntry(key, t.watch);
        case 'mobile':
          return MapEntry(key, t.mobile);
        case 'bag':
          return MapEntry(key, t.bag);
        case 'warehouse':
          return MapEntry(key, t.warehouse);
        case 'addNewPurchase':
          return MapEntry(key, t.addNewPurchase);
        case 'invoiceNo':
          return MapEntry(key, t.invoiceNo);
        case 'selectAWarehouse':
          return MapEntry(key, t.selectAWarehouse);
        case 'items':
          return MapEntry(key, t.items);
        case 'salePrice':
          return MapEntry(key, t.salePrice);
        case 'ex25411':
          return MapEntry(key, t.ex25411);
        case 'supplierName':
          return MapEntry(key, t.supplierName);
        case 'grandTotal':
          return MapEntry(key, t.grandTotal);
        case 'paymentStatus':
          return MapEntry(key, t.paymentStatus);
        case 'salesBy':
          return MapEntry(key, t.salesBy);
        case 'addSales':
          return MapEntry(key, t.addSales);
        case 'scanSearchProductByCodeOrName':
          return MapEntry(key, t.scanSearchProductByCodeOrName);
        case 'selectCustomer':
          return MapEntry(key, t.selectCustomer);
        case 'selectWarehouse':
          return MapEntry(key, t.selectWarehouse);
        case 'totalItem':
          return MapEntry(key, t.totalItem);
        case 'quotation':
          return MapEntry(key, t.quotation);
        case 'payment':
          return MapEntry(key, t.payment);
        case 'welcome':
          return MapEntry(key, t.welcome);
        case 'wall':
          return MapEntry(key, t.wall);
        case 'ceiling':
          return MapEntry(key, t.ceiling);
        case 'door':
          return MapEntry(key, t.door);
        case 'window':
          return MapEntry(key, t.window);
        case 'sofa':
          return MapEntry(key, t.sofa);
        case 'lamp':
          return MapEntry(key, t.lamp);
        default:
          return MapEntry(key, value);
      }
    });
  }
}
